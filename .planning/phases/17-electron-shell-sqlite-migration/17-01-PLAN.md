---
phase: 17-electron-shell-sqlite-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron/main.ts
  - electron/preload.ts
  - nextron.config.js
  - electron-builder.yml
autonomous: true
requirements:
  - ELEC-01
  - ELEC-02
  - ELEC-03

must_haves:
  truths:
    - "pnpm dev launches an Electron BrowserWindow (not a browser tab)"
    - "The window loads the Next.js dev server at http://localhost:3000"
    - "Window dimensions are at least 1280x800"
    - "nodeIntegration is false and contextIsolation is true (security requirement)"
    - "In production mode electron/main.ts injects DATABASE_URL pointing to %APPDATA%\\PremGiriBooks\\data.db before Next.js starts"
  artifacts:
    - path: "electron/main.ts"
      provides: "Electron main process with BrowserWindow creation and DB path injection"
    - path: "electron/preload.ts"
      provides: "Minimal contextBridge preload (security isolation)"
    - path: "nextron.config.js"
      provides: "nextron configuration pointing mainSrcDir to electron/"
    - path: "electron-builder.yml"
      provides: "Minimal app packaging config (dev reference)"
  key_links:
    - from: "electron/main.ts"
      to: "process.env.DATABASE_URL"
      via: "app.getPath('userData') + path.join"
      pattern: "app.getPath\\('userData'\\)"
    - from: "package.json scripts.dev"
      to: "nextron CLI"
      via: "nextron dev command"
      pattern: "\"dev\": \"nextron\""
---

<objective>
Scaffold the Electron shell using nextron so that `pnpm dev` opens a native BrowserWindow instead of a browser tab.

Purpose: Phase 17 converts PremGiri Books from a Vercel-hosted web app to a Windows desktop app. This plan installs nextron + Electron and wires the main process so the app loads in a native window with the correct security settings and DB path injection.

Output: electron/main.ts, electron/preload.ts, nextron.config.js, electron-builder.yml, updated package.json scripts and devDependencies. Running `pnpm dev` opens an Electron window loading http://localhost:3000.
</objective>

<execution_context>
@D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-CONTEXT.md
@D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-RESEARCH.md
</execution_context>

<context>
@D:\My\BPG\design-inspirations-main\package.json
@D:\My\BPG\design-inspirations-main\CLAUDE.md
</context>

<interfaces>
<!-- Key facts extracted from package.json for the executor. -->
Current scripts.dev = "next dev" — must change to "nextron"
Current scripts.build = "prisma migrate deploy && prisma generate && next build" — update in plan 17-03
Package manager: pnpm
Node: v20 LTS
No existing electron/ directory — create from scratch.

nextron v10 conventions (from RESEARCH.md):
- mainSrcDir: "electron" (where electron/main.ts lives)
- nextron passes dev port as process.argv[2]
- BrowserWindow loads http://localhost:{argv[2]}
- Config file: nextron.config.js (CommonJS) at repo root
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install nextron, electron, and electron-builder</name>
  <files>package.json</files>
  <read_first>
    D:\My\BPG\design-inspirations-main\package.json
  </read_first>
  <action>
    Run: pnpm add -D nextron electron electron-builder

    Then update package.json scripts section exactly as follows:
    - "dev": "nextron"  (replaces "next dev")
    - "build:next": "next build"  (preserve the Next.js build under a separate key)
    - "build": "nextron build"  (replaces the old build script)
    - "build:electron": "electron-builder"  (new — for packaging, Phase 22)
    - Keep all other scripts (start, lint, test, seed, etc.) unchanged.

    Do NOT add nextron/electron to dependencies — they belong in devDependencies only.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const ok = p.scripts.dev === 'nextron' && p.devDependencies['nextron'] && p.devDependencies['electron'] && p.devDependencies['electron-builder']; process.exit(ok ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json scripts.dev is exactly "nextron"
    - package.json devDependencies contains nextron, electron, and electron-builder
    - pnpm install completes without errors
  </acceptance_criteria>
  <done>package.json updated with nextron scripts and devDependencies installed</done>
</task>

<task type="auto">
  <name>Task 2: Create electron/main.ts, electron/preload.ts, nextron.config.js, and electron-builder.yml</name>
  <files>
    electron/main.ts
    electron/preload.ts
    nextron.config.js
    electron-builder.yml
  </files>
  <read_first>
    D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-RESEARCH.md
    D:\My\BPG\design-inspirations-main\package.json
  </read_first>
  <action>
    Create electron/ directory and four files.

    FILE: electron/main.ts
    - Import: app, BrowserWindow from 'electron'; path from 'path'
    - Determine isProd via app.isPackaged (NOT process.env.NODE_ENV — isPackaged is correct for nextron)
    - If isProd: set process.env.DATABASE_URL = "file:" + path.join(app.getPath('userData'), 'data.db')
      Else: ensure process.env.DATABASE_URL is set to process.env.DATABASE_URL ?? 'file:./dev.db'
    - Set process.env.DATABASE_URL BEFORE any other logic — it must be set before the Next.js server starts
    - On app.on('ready'): create BrowserWindow with width: 1280, height: 800, minWidth: 1280, minHeight: 800
    - webPreferences: preload = path.join(__dirname, 'preload.js'), nodeIntegration: false, contextIsolation: true
    - Read port from process.argv[2] — nextron injects the dev server port here
    - mainWindow.loadURL("http://localhost:" + port)
    - Handle app.on('window-all-closed'): call app.quit() on non-macOS (process.platform !== 'darwin')
    - Handle app.on('activate'): re-create window on macOS if no windows open

    FILE: electron/preload.ts
    - Import contextBridge from 'electron'
    - Call contextBridge.exposeInMainWorld('electronAPI', {}) — empty object for now (Phase 19 will add IPC)
    - Keep minimal; do NOT expose ipcRenderer directly

    FILE: nextron.config.js (CommonJS — NOT TypeScript, nextron reads this at runtime)
    - module.exports = { mainSrcDir: 'electron', rendererSrcDir: 'app' }
    - Note: rendererSrcDir 'app' tells nextron where the Next.js app lives

    FILE: electron-builder.yml
    - appId: com.premgiribooks.app
    - productName: PremGiri Books
    - directories.output: dist
    - files array: ["electron/**", ".next/**", "public/**", "package.json"]
    - This is a minimal dev-reference config; full packaging is Phase 22

    SECURITY: nodeIntegration must be false and contextIsolation must be true in BrowserWindow webPreferences — never relax these settings.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const main = fs.readFileSync('./electron/main.ts', 'utf8'); const hasNI = main.includes('nodeIntegration: false'); const hasCI = main.includes('contextIsolation: true'); const hasProd = main.includes('app.isPackaged'); const hasDB = main.includes('DATABASE_URL'); process.exit(hasNI && hasCI && hasProd && hasDB ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - electron/main.ts exists and contains: nodeIntegration: false, contextIsolation: true, app.isPackaged, DATABASE_URL injection
    - electron/preload.ts exists and exports contextBridge.exposeInMainWorld
    - nextron.config.js exists with mainSrcDir: 'electron'
    - electron-builder.yml exists with appId: com.premgiribooks.app
    - pnpm dev command starts without immediate crash (Electron window attempts to open)
  </acceptance_criteria>
  <done>Electron shell files created; pnpm dev opens a BrowserWindow loading http://localhost:3000</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main | UI (BrowserWindow) cannot access Node.js APIs directly |
| Main process → filesystem | DB path derived only from app.getPath('userData'), never from renderer input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | BrowserWindow webPreferences | mitigate | nodeIntegration: false, contextIsolation: true — enforced in electron/main.ts |
| T-17-02 | Tampering | DATABASE_URL path | mitigate | Path derived from app.getPath('userData') in main process only; renderer cannot influence it |
| T-17-SC | Tampering | npm installs (nextron, electron, electron-builder) | accept | All three packages passed slopcheck [OK] per RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
After plan execution:
1. Run: pnpm dev — an Electron BrowserWindow must appear (not a browser tab)
2. Confirm window title and URL bar shows http://localhost:3000
3. Run: node -e "const p = require('./package.json'); console.log(p.scripts.dev)" — must print "nextron"
4. Run: node -e "const fs = require('fs'); const c = fs.readFileSync('./electron/main.ts','utf8'); console.log(c.includes('nodeIntegration: false'))" — must print "true"
</verification>

<success_criteria>
- pnpm dev opens a native Electron window (BrowserWindow) — not a browser
- Window minimum size enforced at 1280x800
- nodeIntegration: false and contextIsolation: true verified in source
- DATABASE_URL injection logic present in electron/main.ts using app.isPackaged
- package.json devDependencies includes nextron, electron, electron-builder
</success_criteria>

<output>
Create D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-01-SUMMARY.md when done
</output>
