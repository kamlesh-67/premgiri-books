---
phase: 17-electron-shell-sqlite-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - lib/services/VoucherEngine.ts
  - lib/services/VoucherEngine.test.ts
  - prisma/seed.ts
  - prisma/migrations/
autonomous: true
requirements:
  - DB-02
  - DB-04

must_haves:
  truths:
    - "No SELECT FOR UPDATE SQL appears in VoucherEngine.ts or any other source file"
    - "pnpm prisma migrate dev --name init-sqlite completes without errors"
    - "All tables listed in CLAUDE.md (16 core + extended models) exist in dev.db after migration"
    - "prisma/seed.ts exists with minimal account groups seed (no demo data)"
    - "pnpm dev opens Electron window and the Next.js app loads in it"
    - "A Prisma query against dev.db succeeds (smoke test via API route)"
  artifacts:
    - path: "lib/services/VoucherEngine.ts"
      provides: "SQLite-safe getNextVoucherNo with no FOR UPDATE"
      contains: "getNextVoucherNo"
    - path: "prisma/migrations/"
      provides: "init-sqlite migration SQL files"
    - path: "prisma/seed.ts"
      provides: "Minimal account groups seed"
  key_links:
    - from: "lib/services/VoucherEngine.ts getNextVoucherNo"
      to: "tx.voucherSequence.upsert + tx.voucherSequence.update"
      via: "Prisma $transaction Serializable isolation (no FOR UPDATE needed)"
      pattern: "upsert.*voucherSequence"
    - from: "prisma/migrations/"
      to: "dev.db"
      via: "prisma migrate dev"
      pattern: "init-sqlite"
---

<objective>
Complete the SQLite migration: remove SELECT FOR UPDATE from VoucherEngine, run `prisma migrate dev --name init-sqlite` to create all tables in dev.db, fix any Json default migration bugs, and write a minimal seed script.

Purpose: This is the integration wave that connects the Electron shell (Plan 17-01) with the SQLite schema (Plan 17-02). After this plan, `pnpm dev` opens a desktop window backed by a working SQLite database.

Output: VoucherEngine.ts with SQLite-safe sequence generation; fresh SQLite migration files; seed.ts with account groups; verified smoke test confirming the window opens and the DB is reachable.
</objective>

<execution_context>
@D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-CONTEXT.md
@D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-RESEARCH.md
</execution_context>

<context>
@D:\My\BPG\design-inspirations-main\lib\services\VoucherEngine.ts
@D:\My\BPG\design-inspirations-main\lib\services\VoucherEngine.test.ts
@D:\My\BPG\design-inspirations-main\prisma\schema.prisma
@D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-01-SUMMARY.md
@D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-02-SUMMARY.md
</context>

<interfaces>
<!-- Key facts from VoucherEngine.ts extracted during planning. -->

Current PrismaTx interface (lib/services/VoucherEngine.ts lines ~135-146):
  - Has $executeRaw field — must be removed from the interface after FOR UPDATE removal
  - Has voucherSequence.upsert, voucherSequence.findFirstOrThrow, voucherSequence.update — keep all

Current getNextVoucherNo logic (lines ~387-420):
  Step 1: tx.voucherSequence.upsert (create if missing, update: {})
  Step 2: tx.$executeRaw`SELECT id FROM voucher_sequences WHERE id = ${seqRow.id} FOR UPDATE`  ← REMOVE THIS
  Step 3: tx.voucherSequence.findFirstOrThrow to re-read the row
  Step 4: compute nextSeq = lockedRow.lastSequence + 1
  Step 5: tx.voucherSequence.update with nextSeq
  After removing Step 2, Steps 1, 3, 4, 5 remain unchanged — this is safe on SQLite.

VoucherEngine.test.ts has tests asserting $executeRaw is called — those must be updated:
  - Line ~293: 'calls $executeRaw with SELECT FOR UPDATE to acquire row lock' — update this test
  - Line ~936: 'calls $executeRaw on every invocation to acquire SELECT FOR UPDATE row lock' — update
  - Line ~297: expect(tx.$executeRaw).toHaveBeenCalled() — remove or change to verify it is NOT called
  - Line ~944: expect(tx.$executeRaw).toHaveBeenCalledTimes(2) — remove or update

Json fields with @default("{}") in schema (identified during planning):
  - model Role: permissions Json @default("{}")
  - model Notification: metadata Json @default("{}")
  After migration, inspect prisma/migrations/*/migration.sql for DEFAULT '{}' — fix to DEFAULT '{}' as TEXT if Prisma generates invalid syntax.

Existing prisma/migrations/ directory may contain PostgreSQL migration files — reset it:
  Run: pnpm prisma migrate reset --force (to delete old pg migrations and recreate from scratch)
  Then: pnpm prisma migrate dev --name init-sqlite

ACCOUNT GROUPS for seed.ts (standard Indian accounting chart — from RESEARCH.md):
  Capital Account (LIABILITY), Current Assets (ASSET), Current Liabilities (LIABILITY),
  Sales Accounts (INCOME, affectsGP: true), Purchase Accounts (EXPENSE, affectsGP: true),
  Direct Expenses (EXPENSE, affectsGP: true), Indirect Expenses (EXPENSE, affectsGP: false),
  Indirect Income (INCOME, affectsGP: false), Fixed Assets (ASSET),
  Bank Accounts (ASSET), Cash-in-Hand (ASSET), Sundry Debtors (ASSET),
  Sundry Creditors (LIABILITY), Duties & Taxes (LIABILITY)
  All have isSystem: true; they require a companyId — seed is a no-op placeholder (companyId comes from Phase 19).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove SELECT FOR UPDATE from VoucherEngine.ts and update its test</name>
  <files>
    lib/services/VoucherEngine.ts
    lib/services/VoucherEngine.test.ts
  </files>
  <read_first>
    D:\My\BPG\design-inspirations-main\lib\services\VoucherEngine.ts
    D:\My\BPG\design-inspirations-main\lib\services\VoucherEngine.test.ts
  </read_first>
  <action>
    FILE: lib/services/VoucherEngine.ts

    1. In the PrismaTx interface (around line 135), remove the $executeRaw field:
         $executeRaw: (query: TemplateStringsArray, ...values: unknown[]) => Promise<number>
       The interface no longer needs it. Keep all other fields (voucherSequence, voucher, etc.) unchanged.

    2. In the function getNextVoucherNo (around line 387), remove Step 2 entirely:
       Remove the line: await tx.$executeRaw`SELECT id FROM voucher_sequences WHERE id = ${seqRow.id} FOR UPDATE`
       
    3. Update the comment block above getNextVoucherNo:
       Remove any reference to "SELECT FOR UPDATE" from the JSDoc.
       Add: "SQLite serializes $transaction automatically — no explicit row lock needed."

    4. Update the comment at the top of the file (around line 6) that says "1. Sequence generation with SELECT FOR UPDATE":
       Change to: "1. Sequence generation with Prisma $transaction (SQLite Serializable isolation — no FOR UPDATE needed)"

    FILE: lib/services/VoucherEngine.test.ts

    5. Find and update every test that asserts $executeRaw is called for the SELECT FOR UPDATE:
       - Test around line 293: 'calls $executeRaw with SELECT FOR UPDATE to acquire row lock'
         Change the test name to 'does NOT call $executeRaw — SQLite $transaction is Serializable'
         Change the assertion from expect(tx.$executeRaw).toHaveBeenCalled() 
         to expect(tx.$executeRaw).not.toHaveBeenCalled()
       
       - Test around line 936: 'calls $executeRaw on every invocation to acquire SELECT FOR UPDATE row lock'
         Change to: 'does NOT call $executeRaw on any invocation — SQLite serializes $transaction'
         Change expect(tx.$executeRaw).toHaveBeenCalledTimes(2)
         to expect(tx.$executeRaw).not.toHaveBeenCalled()
       
       - Any other test asserting $executeRaw is called for FOR UPDATE: apply the same inversion.
       
    6. The mock object `tx` in tests still has `$executeRaw: vi.fn().mockResolvedValue(1)` — keep the mock defined on the tx object (it does no harm), only change the assertions that previously expected it to be called.

    Run the test suite after changes: pnpm vitest run lib/services/VoucherEngine.test.ts
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const src = fs.readFileSync('./lib/services/VoucherEngine.ts','utf8'); const noFU = !src.includes('FOR UPDATE'); const noExecRaw = !src.includes('$executeRaw'); process.exit(noFU && noExecRaw ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - grep "FOR UPDATE" lib/services/VoucherEngine.ts returns no matches
    - grep "\$executeRaw" lib/services/VoucherEngine.ts returns no matches
    - PrismaTx interface no longer has $executeRaw field
    - pnpm vitest run lib/services/VoucherEngine.test.ts passes (all tests green)
    - getNextVoucherNo still performs upsert + findFirstOrThrow + update (logic intact, only lock removed)
  </acceptance_criteria>
  <done>VoucherEngine.ts has no SELECT FOR UPDATE; tests updated and passing</done>
</task>

<task type="auto">
  <name>Task 2: Run SQLite migration, fix Json defaults, write seed.ts, smoke-test the app</name>
  <files>
    prisma/seed.ts
    prisma/migrations/
  </files>
  <read_first>
    D:\My\BPG\design-inspirations-main\prisma\schema.prisma
    D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-RESEARCH.md
  </read_first>
  <action>
    STEP 1 — Reset existing migrations (old PostgreSQL migration files will conflict):
    Run: pnpm prisma migrate reset --force
    This drops the old migrations folder contents. Data loss is acceptable (per CONTEXT.md).

    STEP 2 — Run initial SQLite migration:
    Run: pnpm prisma migrate dev --name init-sqlite
    Expected: Prisma generates prisma/migrations/{timestamp}_init-sqlite/migration.sql and creates dev.db

    STEP 3 — Inspect generated migration.sql for Json default bugs (Prisma issue #26571):
    Read the generated migration.sql file.
    Search for any DEFAULT '{}' or DEFAULT "{}" on TEXT columns.
    If Prisma generated invalid DEFAULT syntax for the Json fields (Role.permissions or Notification.metadata),
    manually edit the migration.sql to correct the DEFAULT expression to a valid SQLite TEXT literal: DEFAULT '{}'.
    After any manual fix, re-run: pnpm prisma migrate dev (it will apply the corrected SQL).

    STEP 4 — Generate Prisma client:
    Run: pnpm prisma generate
    Expected: "Generated Prisma Client" with no errors.

    STEP 5 — Write prisma/seed.ts:
    Create a minimal seed that defines the standard Indian account groups but does NOT insert them
    (inserting requires a companyId which does not exist until Phase 19's first-run wizard).
    The seed should:
    - Import PrismaClient from '@prisma/client'
    - Define an ACCOUNT_GROUPS constant array with the 14 groups listed in the interfaces section
    - Export a main() function that logs "Seed: schema-only run. Company-level seed runs post first-run wizard (Phase 19)." and exits
    - Call main().catch(console.error).finally(() => prisma.$disconnect())
    - Include a comment at the top explaining this is a placeholder

    STEP 6 — Verify table count:
    Run: node -e "const { execSync } = require('child_process'); const out = execSync('pnpm prisma db execute --stdin <<< \".tables\"', { shell: true }); console.log(out.toString())"
    Alternative check — use sqlite3 CLI if available:
    Run: pnpm prisma db execute --url "file:./dev.db" --stdin < /dev/null
    Or: node -e "const Database = require('better-sqlite3'); const db = new Database('./dev.db'); const tables = db.prepare(\"SELECT name FROM sqlite_master WHERE type='table'\").all(); console.log(tables.length, tables.map(t=>t.name).join(', '))"

    Actually: use prisma studio as a non-interactive check — skip table count via node; instead verify via pnpm prisma validate which was already done in Plan 17-02. The migration success itself is the acceptance criteria.

    STEP 7 — Smoke test (pnpm dev):
    Run pnpm dev and verify:
    - Electron window opens
    - Navigate to http://localhost:3000/api/v1/health (or any health endpoint if it exists) — verify the endpoint returns HTTP 200 with a SQLite-backed response.
    - If no health endpoint exists, confirm the DB is reachable by running: node -e "const Database = require('better-sqlite3'); const db = new Database('./dev.db'); const rows = db.prepare('SELECT COUNT(*) as c FROM vouchers').all(); db.close(); console.log('vouchers table row count:', rows[0].c); process.exit(0)"
    - This verifies the vouchers table exists and Prisma can query SQLite (ELEC-02 functional verification).
    - Confirm dev.db file was created at the project root (for dev mode)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const migDir = fs.readdirSync('./prisma/migrations').filter(f => f !== 'migration_lock.toml'); const hasMig = migDir.length > 0; const hasDb = fs.existsSync('./dev.db'); const hasSeed = fs.existsSync('./prisma/seed.ts'); if (!hasMig || !hasDb || !hasSeed) process.exit(1); const Database = require('better-sqlite3'); const db = new Database('./dev.db'); const tables = db.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'").all(); db.close(); console.log('Tables found:', tables.length, tables.map(t=>t.name).join(', ')); process.exit(tables.length >= 14 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm prisma migrate dev --name init-sqlite exits 0
    - prisma/migrations/ contains at least one timestamped directory with migration.sql
    - dev.db file exists at project root after migration
    - pnpm prisma generate exits 0
    - prisma/seed.ts exists with ACCOUNT_GROUPS constant defined
    - pnpm dev opens Electron window (manual verification)
    - No "FOR UPDATE" syntax errors in migration.sql
    - Json DEFAULT expressions in migration.sql are valid SQLite TEXT literals
  </acceptance_criteria>
  <done>SQLite migration complete; dev.db exists with all tables; seed.ts written; Electron window opens and app loads</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma $transaction → SQLite | All writes go through Prisma ORM; SQLite Serializable isolation handles concurrency |
| seed.ts → SQLite | Seed is a no-op placeholder; no untrusted data inserted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering | VoucherSequence concurrent writes | accept | SQLite $transaction is Serializable — duplicate voucher numbers are prevented without explicit FOR UPDATE |
| T-17-06 | Denial of Service | Json @default("{}") migration bug | mitigate | Inspect generated migration.sql and manually fix any invalid DEFAULT expressions before applying |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After plan execution run these checks in order:
1. grep "FOR UPDATE" lib/services/VoucherEngine.ts — must return empty
2. pnpm vitest run lib/services/VoucherEngine.test.ts — all tests green
3. ls prisma/migrations/ — must contain at least one directory
4. ls dev.db — file must exist
5. pnpm prisma generate — must exit 0
6. pnpm dev — Electron window opens, app loads without console errors
7. Manual: navigate to /dashboard — page renders (even if data is empty)
</verification>

<success_criteria>
- VoucherEngine.ts: zero references to FOR UPDATE or $executeRaw
- VoucherEngine.test.ts: all tests pass (assertions updated to expect $executeRaw NOT called)
- prisma migrate dev --name init-sqlite completed with all tables created
- dev.db exists at project root
- pnpm dev opens Electron window (ELEC-01 satisfied)
- app loads http://localhost:3000 in BrowserWindow (ELEC-02 satisfied)
- dev.db is the active database file (DB-04 satisfied — all tables migrated)
</success_criteria>

<output>
Create D:\My\BPG\design-inspirations-main\.planning\phases\17-electron-shell-sqlite-migration\17-03-SUMMARY.md when done
</output>
