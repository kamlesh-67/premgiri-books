---
phase: "19"
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - app/page.tsx
  - app/(auth)/setup/page.tsx
  - app/(auth)/setup/SetupWizard.tsx
autonomous: true
requirements:
  - SETUP-01
  - SETUP-05

must_haves:
  truths:
    - "Navigating to / with zero companies redirects the browser to /setup"
    - "Navigating to / with a company + valid session redirects to /dashboard"
    - "Navigating to / with a company + no session redirects to /login"
    - "Navigating to /setup with a company already in DB redirects to /login immediately (no wizard flash)"
    - "Setup wizard renders a two-step form: Step 1 collects company fields, Step 2 collects admin password"
    - "GSTIN entry auto-populates the stateCode field from the first two GSTIN digits"
    - "Successful setup POST redirects the user to /dashboard (auto-login, no separate login step)"
    - "Toast error appears when the setup POST returns a non-2xx response"
  artifacts:
    - path: "app/page.tsx"
      provides: "Root redirect — zero-company check before session check"
      contains: "authDb.company.count"
    - path: "app/(auth)/setup/page.tsx"
      provides: "Server Component page guard — redirects to /login if company exists"
      contains: "redirect('/login')"
    - path: "app/(auth)/setup/SetupWizard.tsx"
      provides: "Multi-step wizard client component"
      exports: ["SetupWizard"]
  key_links:
    - from: "app/(auth)/setup/SetupWizard.tsx"
      to: "POST /api/v1/setup"
      via: "fetch in handleAdminSubmit"
      pattern: "fetch\\('/api/v1/setup'"
    - from: "app/(auth)/setup/page.tsx"
      to: "lib/authDb.ts"
      via: "authDb.company.count()"
      pattern: "authDb\\.company\\.count"
    - from: "app/page.tsx"
      to: "lib/authDb.ts"
      via: "authDb.company.count()"
      pattern: "authDb\\.company\\.count"
---

<objective>
Build the frontend side of the first-run setup wizard: root page redirect logic, setup page guard, and the multi-step wizard client component.

Purpose: Plan 01 created the API. This plan wires the browser flow: zero-company → /setup → wizard → POST → /dashboard. Without this, there is no user-facing path to trigger setup.

Output:
- app/page.tsx — async Server Component that routes first-timers to /setup and returning users to /dashboard or /login
- app/(auth)/setup/page.tsx — thin Server Component guard (redirect to /login if already set up)
- app/(auth)/setup/SetupWizard.tsx — 'use client' two-step form (company fields → admin password → POST → router.push)
</objective>

<execution_context>
@/home/kamlesh/.claude/get-shit-done/workflows/execute-plan.md
@/home/kamlesh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-first-run-company-setup-wizard/19-RESEARCH.md
@.planning/phases/19-first-run-company-setup-wizard/19-PATTERNS.md
@.planning/phases/19-first-run-company-setup-wizard/19-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts extracted from codebase. Use these directly. -->

From lib/session.ts:
  export async function readSession(): Promise<JWTPayload | null>
  // Reads auth-token cookie via next/headers cookies() — use in Server Components

From lib/authDb.ts:
  export const authDb  // un-tenanted PrismaClient — use for pre-session DB calls

From app/(auth)/login/page.tsx (UI pattern to mirror):
  Split-screen layout: flex h-screen
  LEFT: w-1/2 bg-purple-600 flex-col justify-between p-10
  RIGHT: w-full md:w-1/2 bg-white flex items-center justify-center
  Inner content: w-full max-w-sm px-8
  FormField pattern: FormField > FormItem > FormLabel + FormControl(Input) + FormMessage
  Loading button: disabled={isLoading} className="w-full bg-purple-600 hover:bg-purple-700 text-white"
  Toast error: toast.error('...', { duration: 4000 })
  Submit fetch: await fetch('/api/v1/auth/login', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({...}) })
  After success: router.push('/company-select'); router.refresh()

From CLAUDE.md design system:
  Primary: bg-purple-600 (#7C3AED) for buttons and active states
  Text heading: text-gray-900
  Text body: text-gray-700
  Text secondary: text-gray-500
  Border: border-gray-200
  FormLabel: text-sm font-medium text-gray-700
  FormMessage: text-xs text-red-600
  shadcn/ui components: Button, Input, Form, FormControl, FormField, FormItem, FormLabel, FormMessage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app/page.tsx — add zero-company redirect before session check</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx — current state is 4 lines: import redirect, sync default export that calls redirect('/dashboard')
    - lib/session.ts — readSession() signature; it is async and reads from next/headers
    - lib/authDb.ts — confirm the authDb export name
  </read_first>
  <action>
    Replace app/page.tsx entirely. The new file is an async Server Component (no 'use client').

    Imports:
    - redirect from 'next/navigation'
    - readSession from '@/lib/session'
    - authDb from '@/lib/authDb'

    Logic (in order):
    1. const count = await authDb.company.count()
    2. if (count === 0) redirect('/setup')
    3. const session = await readSession()
    4. if (!session) redirect('/login')
    5. redirect('/dashboard')

    The function must be exported as default and marked async. No JSX — the component only calls redirect() and never renders markup.

    Rationale: The current page.tsx always redirects to /dashboard. Adding the company count check first means first-timers hit /setup before ever seeing /login. This satisfies SETUP-01.
  </action>
  <verify>
    <automated>grep -n "company.count\|readSession\|redirect" app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains authDb.company.count()
    - app/page.tsx contains redirect('/setup') (zero-company branch)
    - app/page.tsx contains readSession() (existing-company branch)
    - app/page.tsx contains redirect('/login') (no-session branch)
    - app/page.tsx contains redirect('/dashboard') (authenticated branch)
    - Default export is async
    - pnpm build exits 0
  </acceptance_criteria>
  <done>Root page routes first-timers to /setup, authenticated users to /dashboard, and unauthenticated returning users to /login</done>
</task>

<task type="auto">
  <name>Task 2: Create app/(auth)/setup/page.tsx — Server Component guard</name>
  <files>app/(auth)/setup/page.tsx</files>
  <read_first>
    - app/(auth)/login/page.tsx — structure of an auth-group page (this new file is a thin wrapper, unlike login which is also the UI; setup delegates UI to SetupWizard)
    - lib/authDb.ts — authDb export name
  </read_first>
  <action>
    Create app/(auth)/setup/page.tsx. This is a Server Component (no 'use client'). The directory app/(auth)/setup/ must be created.

    Imports:
    - redirect from 'next/navigation'
    - authDb from '@/lib/authDb'
    - SetupWizard (named export) from './SetupWizard'

    Logic:
    1. const count = await authDb.company.count()
    2. if (count > 0) redirect('/login')
    3. return <SetupWizard />

    The function must be exported as default and marked async.

    There is no metadata export — login page has none either, follow the same pattern.

    Rationale: The Server Component guard prevents the wizard from flashing before a redirect fires. A user who bookmarks /setup after setup is complete is immediately sent to /login without seeing any wizard markup. This is the server-side half of SETUP-05.
  </action>
  <verify>
    <automated>grep -n "count\|redirect\|SetupWizard" "app/(auth)/setup/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/(auth)/setup/page.tsx
    - File contains authDb.company.count()
    - File contains redirect('/login') triggered when count > 0
    - File renders SetupWizard when count === 0
    - Default export is async
    - pnpm build exits 0
  </acceptance_criteria>
  <done>app/(auth)/setup/page.tsx redirects to /login when a company exists; renders SetupWizard when DB is empty</done>
</task>

<task type="auto">
  <name>Task 3: Create app/(auth)/setup/SetupWizard.tsx — two-step wizard client component</name>
  <files>app/(auth)/setup/SetupWizard.tsx</files>
  <read_first>
    - app/(auth)/login/page.tsx — copy the split-screen layout (lines 60-174), FormField pattern (lines 103-147), loading button (lines 150-163), toast error (lines 44-49), and router.push + router.refresh (lines 50-53) exactly; adapt text and fields
  </read_first>
  <action>
    Create app/(auth)/setup/SetupWizard.tsx as a named export (not default) Client Component.

    File begins with 'use client' directive.

    Imports (follow login page pattern, adding useEffect and Select components):
    - useState, useEffect from 'react'
    - useForm from 'react-hook-form'
    - zodResolver from '@hookform/resolvers/zod'
    - useRouter from 'next/navigation'
    - toast from 'sonner'
    - Loader2 from 'lucide-react'
    - z from 'zod'
    - Button, Input from '@/components/ui/*'
    - Form, FormControl, FormField, FormItem, FormLabel, FormMessage from '@/components/ui/form'
    - Select, SelectContent, SelectItem, SelectTrigger, SelectValue from '@/components/ui/select'

    Two Zod schemas:
    1. companySchema — fields: companyName (string min 1 max 200), gstin (optional string matching GSTIN regex or empty string), pan (optional string matching PAN regex or empty string), address (optional string max 500), stateCode (string length 2), fyStart (number int min 1 max 12 default 4)
    2. adminSchema — fields: adminPassword (string min 8, message "Password must be at least 8 characters"), confirmPassword (string min 1)
       Plus .refine((d) => d.adminPassword === d.confirmPassword, { message: 'Passwords do not match', path: ['confirmPassword'] })

    Type aliases:
    - CompanyFormData = z.infer<typeof companySchema>
    - AdminFormData = z.infer<typeof adminSchema>
    - WizardStep = 'company' | 'admin'

    Component signature: export function SetupWizard() (named export, not default)

    State:
    - step: WizardStep — useState<WizardStep>('company')
    - companyData: CompanyFormData | null — useState<CompanyFormData | null>(null)
    - isLoading: boolean — useState(false)

    Two useForm instances, each in the component scope:
    - companyForm: useForm<CompanyFormData> with zodResolver(companySchema), defaultValues { companyName:'', gstin:'', pan:'', address:'', stateCode:'', fyStart:4 }
    - adminForm: useForm<AdminFormData> with zodResolver(adminSchema), defaultValues { adminPassword:'', confirmPassword:'' }

    GSTIN → stateCode auto-derive (in company step context):
    - const gstin = companyForm.watch('gstin')
    - useEffect: if (gstin && gstin.length >= 2) companyForm.setValue('stateCode', gstin.substring(0, 2))
    - Dependency array: [gstin]

    Handlers:
    - handleCompanyNext(data: CompanyFormData): setCompanyData(data); setStep('admin')
    - handleAdminSubmit(data: AdminFormData): setIsLoading(true) in try block; fetch POST /api/v1/setup with JSON body { ...companyData, adminPassword: data.adminPassword }; if !res.ok → toast.error('Setup failed. Please try again.', { duration: 4000 }) and return; router.push('/dashboard'); router.refresh(); finally: setIsLoading(false)

    Layout — SAME split-screen structure as login page:
    - Outer: div className="flex h-screen"
    - Left panel: div className="hidden md:flex w-1/2 bg-purple-600 flex-col justify-between p-10"
      - Top: span "PremGiri Books" text-white text-2xl font-bold
      - Middle: p "Welcome to PremGiri Books" text-white text-xl font-semibold mt-8 max-w-xs leading-snug; p "Let's set up your company" text-sm text-purple-200 mt-2
      - Bottom: p "© 2025 PremGiri Books" text-xs text-purple-300
    - Right panel: div className="w-full md:w-1/2 bg-white flex items-center justify-center"
      - Inner: div className="w-full max-w-sm px-8"
        - Step indicator (text only, not a progress bar): p "Step {step === 'company' ? 1 : 2} of 2" text-xs text-gray-400 mb-1
        - Step heading: p className="text-xl font-bold text-gray-900 mb-1" — "Set up your company" (step 1) OR "Create admin account" (step 2)
        - Subtitle: p className="text-sm text-gray-500 mb-6"

    Step 1 form (when step === 'company') — uses companyForm, calls handleCompanyNext on submit:
    Fields in order:
    1. companyName — label "Company Name", placeholder "Sharma Trading Co."
    2. gstin — label "GSTIN (optional)", placeholder "27AAPFU0939F1ZV"
    3. pan — label "PAN (optional)", placeholder "AAPFU0939F"
    4. address — label "Address (optional)", placeholder "123 Main Street, Mumbai"
    5. stateCode — label "State Code", placeholder "27" (2-char string; auto-filled from GSTIN)
    6. fyStart — label "Financial Year Start Month" — render as a Select (not Input) with options 1-12; display month names ("April (4)", "January (1)", etc.); default shown as "April (4)"
    Submit button: text "Next →", no loading spinner (not calling API yet)
    Back button: not shown on step 1

    Step 2 form (when step === 'admin') — uses adminForm, calls handleAdminSubmit on submit:
    Fields in order:
    1. adminPassword — label "Admin Password", type="password", placeholder "••••••••"
    2. confirmPassword — label "Confirm Password", type="password", placeholder "••••••••"
    Below fields (not inside FormField): p className="text-xs text-gray-400 mt-1" — "Email: admin@premgiribooks.com (fixed)"
    Submit button: same loading pattern as login page ("Setting up..." with Loader2 spinner when isLoading)
    Back button: Button variant="outline" onClick={() => setStep('company')} — "← Back", placed before submit button, full width, className="w-full mb-2"

    FormField pattern for all text inputs: copy exactly from login page (FormField > FormItem > FormLabel text-sm font-medium text-gray-700 > FormControl > Input className="border-gray-200" > FormMessage className="text-xs text-red-600")

    Do NOT render both steps simultaneously — use conditional rendering: {step === 'company' ? <CompanyForm /> : <AdminForm />} or inline JSX conditionals. Keep the left panel always visible regardless of step.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists at app/(auth)/setup/SetupWizard.tsx
    - File begins with 'use client'
    - File contains export function SetupWizard (named export)
    - File contains two useForm calls (companyForm and adminForm)
    - File contains useEffect watching gstin and calling companyForm.setValue('stateCode', gstin.substring(0, 2))
    - File contains fetch('/api/v1/setup' with method: 'POST'
    - File contains router.push('/dashboard') and router.refresh()
    - File contains toast.error for non-ok responses
    - File contains "Step" indicator text with step number
    - File contains type="password" for both password fields
    - File contains .refine check for password confirmation
    - pnpm build exits 0 with no TypeScript errors in any of the three files created/modified in this plan
  </acceptance_criteria>
  <done>Two-step wizard collects company info and admin password, auto-derives stateCode from GSTIN, submits to POST /api/v1/setup, auto-logs in on success and redirects to /dashboard, shows toast on error, and has a Back button on step 2</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Component (app/page.tsx) | SSR request; DB read for company count is trusted server-side |
| Browser → Server Component (setup/page.tsx) | SSR guard; same trust level |
| Client Component → POST /api/v1/setup | Client-submitted fetch; validated server-side by Plan 01's route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Elevation of Privilege | /setup page after setup complete | mitigate | Server Component page guard: count > 0 → redirect('/login') before wizard renders — no client code executes |
| T-19-06 | Elevation of Privilege | Bookmarked /setup URL replayed | mitigate | Both page-level guard (Server Component) and API-level guard (409) enforce one-time setup |
| T-19-07 | Information Disclosure | Admin email hardcoded as admin@premgiribooks.com | accept | Desktop app; single-admin model; email is a fixed constant not a secret |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 19 — all deps already verified in Phase 18 |
</threat_model>

<verification>
After all three tasks complete:

1. pnpm build exits 0 — no TypeScript errors across all three files
2. grep -n "company.count\|readSession\|redirect" app/page.tsx — shows all four branches (setup, login, dashboard)
3. grep -n "count\|redirect\|SetupWizard" "app/(auth)/setup/page.tsx" — shows guard and render
4. grep -c "useForm" "app/(auth)/setup/SetupWizard.tsx" — outputs 2 (two form instances)
5. grep -n "fetch.*api/v1/setup" "app/(auth)/setup/SetupWizard.tsx" — shows the POST call
</verification>

<success_criteria>
- app/page.tsx: async Server Component with company count → /setup branch + session check branches
- app/(auth)/setup/page.tsx: async Server Component with count > 0 → redirect('/login') guard
- app/(auth)/setup/SetupWizard.tsx: 'use client', two Zod schemas, two useForm instances, GSTIN→stateCode auto-derive, fetch POST /api/v1/setup, router.push('/dashboard') on success, toast.error on failure, two-step conditional render matching login page split-screen layout
- pnpm build passes with no TypeScript errors
</success_criteria>

<output>
Create .planning/phases/19-first-run-company-setup-wizard/19-02-SUMMARY.md when done
</output>
