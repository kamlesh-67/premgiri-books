---
phase: 20-user-management
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - components/layout/AppSidebar.tsx
  - app/(app)/settings/users/page.tsx
  - app/(app)/settings/users/UsersPageClient.tsx
  - components/settings/ResetPasswordDialog.tsx
autonomous: true
requirements:
  - USER-04
  - USER-05

must_haves:
  truths:
    - "Navigating to /settings/users as Accountant role renders an access-denied message (not a redirect to /login)"
    - "AppSidebar hides the Users nav item when the permissions prop lacks users.read"
    - "AppSidebar shows the Users nav item when the permissions prop includes users.read"
    - "ResetPasswordDialog calls POST /api/v1/users/:id/reset-password and shows success or error"
  artifacts:
    - path: "components/layout/AppSidebar.tsx"
      provides: "isPermitted() filter applied to nav items alongside isVisible()"
      contains: "isPermitted"
    - path: "app/(app)/settings/users/page.tsx"
      provides: "Server Component wrapper with readSession + hasPermission 403 guard"
      contains: "hasPermission"
    - path: "app/(app)/settings/users/UsersPageClient.tsx"
      provides: "Client component renamed from original page.tsx — keeps 'use client'"
      contains: "use client"
    - path: "components/settings/ResetPasswordDialog.tsx"
      provides: "Dialog for resetting another user's password via reset-password endpoint"
      exports: ["ResetPasswordDialog"]
  key_links:
    - from: "app/(app)/settings/users/page.tsx"
      to: "lib/session.ts"
      via: "readSession()"
      pattern: "readSession"
    - from: "app/(app)/settings/users/page.tsx"
      to: "lib/services/PermissionService.ts"
      via: "hasPermission()"
      pattern: "hasPermission"
    - from: "components/layout/AppSidebar.tsx"
      to: "lib/services/PermissionService.ts"
      via: "hasPermission()"
      pattern: "hasPermission"
    - from: "components/settings/ResetPasswordDialog.tsx"
      to: "/api/v1/users/[id]/reset-password"
      via: "fetch POST"
      pattern: "reset-password"
---

<objective>
Deliver the frontend layer of user management: server-side 403 guard on the Users page, sidebar
permission filtering to hide the Users nav item from Accountant users, and the ResetPasswordDialog
component so Owners can reset another user's password from the UI.

Purpose: This completes the full USER-04 and USER-05 requirements. The backend endpoints from
Plan 01 and Plan 02 are all in place — this plan wires the UI and server-side guards.

Output: Users page converted to a Server Component wrapper with 403 guard; UsersPageClient.tsx
created (renamed existing page.tsx); AppSidebar extended with isPermitted filter; new
ResetPasswordDialog component.
</objective>

<execution_context>
@D:\My\BPG\design-inspirations-main\.claude\get-shit-done\workflows\execute-plan.md
@D:\My\BPG\design-inspirations-main\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\My\BPG\design-inspirations-main\.planning\PROJECT.md
@D:\My\BPG\design-inspirations-main\.planning\ROADMAP.md
@D:\My\BPG\design-inspirations-main\.planning\STATE.md
@D:\My\BPG\design-inspirations-main\.planning\phases\20-user-management\20-01-SUMMARY.md
@D:\My\BPG\design-inspirations-main\.planning\phases\20-user-management\20-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types/patterns the executor needs -->

From lib/services/PermissionService.ts:
  export function hasPermission(permissions: unknown, resource: string, action: string): boolean
  // permissions shape: { [resource]: string[] }  e.g. { users: ['read', 'admin'] }

From lib/session.ts (readSession):
  export async function readSession(): Promise<JWTPayload | null>
  // JWTPayload includes: userId, companyId, roleId, role, uiMode, permissions

From components/layout/navConfig.ts (NavItem type):
  interface NavItem {
    href: string
    simpleLabel?: string
    advancedLabel: string
    icon: LucideIcon
    shortcut?: string
    visibleIn: ('simple' | 'advanced')[]
    requirePermission?: { resource: string; action: string }
  }

From components/layout/AppSidebar.tsx (current AppSidebarProps, lines 11-16):
  interface AppSidebarProps {
    companyName: string
    userName: string
    userRole: string
    financialYear: string
  }
  // Plan 01 Task 3 added permissions={session.permissions} call in layout.tsx with @ts-expect-error
  // This task adds the prop to the interface to resolve that type error.

From app/(app)/layout.tsx — session.permissions is passed as prop (added in Plan 01 Task 3)

From components/settings/UserDialog.tsx (dialog pattern to replicate):
  - Uses 'use client' directive
  - useState for form fields and error
  - useEffect to reset form when open changes
  - fetch() for API call with try/finally setSaving pattern
  - Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from @/components/ui/dialog
  - Button, Input, Label from @/components/ui/button, @/components/ui/input, @/components/ui/label
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add permissions prop to AppSidebar and wire isPermitted filter</name>
  <files>components/layout/AppSidebar.tsx</files>
  <read_first>
    - components/layout/AppSidebar.tsx (full file — identify: AppSidebarProps interface lines 11-16, existing isVisible function at ~line 40, the nav filter at ~line 79 that calls group.items.filter(isVisible))
    - lib/services/PermissionService.ts (confirm hasPermission signature and import path)
    - components/layout/navConfig.ts (confirm NavItem.requirePermission field shape: { resource: string; action: string })
  </read_first>
  <action>
    Make the following targeted changes to AppSidebar.tsx:

    1. Add import:
       import { hasPermission } from '@/lib/services/PermissionService'

    2. Extend AppSidebarProps interface — add optional permissions field:
       permissions?: unknown

    3. Destructure permissions in the function signature alongside existing props:
       { companyName, userName, userRole, financialYear, permissions }

    4. Add isPermitted helper function inside the AppSidebar function body, directly after the
       existing isVisible function:

         function isPermitted(item: NavItem): boolean {
           if (!item.requirePermission) return true
           return hasPermission(
             permissions,
             item.requirePermission.resource,
             item.requirePermission.action
           )
         }

    5. Change the nav filter line from:
         const visibleItems = group.items.filter(isVisible)
       to:
         const visibleItems = group.items.filter((item) => isVisible(item) && isPermitted(item))

    Do NOT change the rest of the component. Do NOT remove any existing logic.
  </action>
  <verify>
    <automated>
      grep -n "isPermitted" components/layout/AppSidebar.tsx
    </automated>
    Must return at least 2 lines: the function definition and the filter usage.
  </verify>
  <done>
    - AppSidebarProps has permissions?: unknown field
    - Function signature destructures permissions
    - isPermitted() function is defined inside AppSidebar function body
    - Filter uses (item) => isVisible(item) && isPermitted(item)
    - hasPermission is imported from @/lib/services/PermissionService
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert Users page to Server Component wrapper with 403 guard</name>
  <files>app/(app)/settings/users/page.tsx, app/(app)/settings/users/UsersPageClient.tsx</files>
  <read_first>
    - app/(app)/settings/users/page.tsx (full file — read the existing page content; note whether it has 'use client' at top; identify all imports and JSX so nothing is lost in the rename)
    - app/(app)/layout.tsx (lines 14-21 — the readSession + redirect pattern to replicate)
    - lib/services/PermissionService.ts (hasPermission signature)
  </read_first>
  <action>
    Step 1 — Rename existing page to client component:
    Copy the full content of the existing app/(app)/settings/users/page.tsx into a new file
    app/(app)/settings/users/UsersPageClient.tsx. If the existing page.tsx does NOT start with
    'use client', add 'use client' as the first line of UsersPageClient.tsx. If it already
    starts with 'use client', the content is unchanged. The default export function in
    UsersPageClient.tsx must be named UsersPageClient (rename from whatever it was previously).

    Step 2 — Replace page.tsx with a Server Component wrapper:
    Overwrite app/(app)/settings/users/page.tsx with a new file that:
    - Has NO 'use client' directive (it is a Server Component)
    - Imports: readSession from @/lib/session, redirect from next/navigation,
               hasPermission from @/lib/services/PermissionService,
               UsersPageClient from ./UsersPageClient
    - Exports a default async function UsersPage()
    - Calls: const session = await readSession()
    - If session is null: calls redirect('/login')
    - If !hasPermission(session.permissions, 'users', 'read'):
        return (
          <div className="p-6 flex flex-col items-center justify-center min-h-[50vh] text-center">
            <h2 className="text-xl font-semibold text-gray-900">Access Denied</h2>
            <p className="mt-2 text-sm text-gray-500">
              You need Owner permissions to access user management.
            </p>
          </div>
        )
    - Otherwise: return <UsersPageClient />

    Critical rule: Do NOT call notFound() or redirect('/login') for the 403 case. The user is
    authenticated — only unauthorized. Render the access-denied JSX directly.
  </action>
  <verify>
    <automated>
      grep -n "hasPermission" "app/(app)/settings/users/page.tsx"
    </automated>
    Must return 1 line. Also verify:
    <automated>
      grep -c "use client" "app/(app)/settings/users/UsersPageClient.tsx"
    </automated>
    Must return 1 (file has 'use client' directive).
  </verify>
  <done>
    - app/(app)/settings/users/UsersPageClient.tsx exists with 'use client' at top
    - app/(app)/settings/users/page.tsx is a Server Component (no 'use client')
    - page.tsx imports readSession, hasPermission, UsersPageClient
    - page.tsx renders access-denied JSX (not redirect) when hasPermission returns false
    - page.tsx renders <UsersPageClient /> when permission check passes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create ResetPasswordDialog component and wire into UsersPageClient</name>
  <files>components/settings/ResetPasswordDialog.tsx, app/(app)/settings/users/UsersPageClient.tsx</files>
  <read_first>
    - components/settings/UserDialog.tsx (full file — extract the exact import block for shadcn Dialog/Button/Input/Label, the useState + useEffect reset pattern, the fetch try/finally setSaving pattern, and the DialogContent JSX structure)
    - app/(app)/settings/users/UsersPageClient.tsx (full file — identify where UserDialog is used and how users are listed; find the table row actions to add a "Reset Password" button)
  </read_first>
  <action>
    Step 1 — Create components/settings/ResetPasswordDialog.tsx:

    'use client' directive at top.

    Imports:
    - useState, useEffect from react
    - Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from @/components/ui/dialog
    - Button from @/components/ui/button
    - Input from @/components/ui/input
    - Label from @/components/ui/label

    Props interface:
      export interface ResetPasswordDialogProps {
        open: boolean
        onOpenChange: (open: boolean) => void
        userId: string
        userName: string
        onSuccess: () => void
      }

    State: password (string), confirmPassword (string), error (string | null), saving (boolean)

    useEffect: when open becomes true, reset all state fields to initial values ('', '', null, false)

    handleSubmit:
    - Prevent default
    - Client-side validation: password.length < 8 → setError('Password must be at least 8 characters.'); return
    - Client-side validation: password !== confirmPassword → setError('Passwords do not match.'); return
    - setError(null); setSaving(true)
    - fetch(`/api/v1/users/${userId}/reset-password`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ password }) })
    - if (!response.ok): const body = await response.json(); setError(body?.error ?? 'Something went wrong. Please try again.'); return
    - onSuccess(); onOpenChange(false)
    - finally: setSaving(false)

    Render: Dialog with two password fields (new password, confirm password), error paragraph when error is non-null, Cancel and "Reset Password" buttons. Title: "Reset Password — {userName}"

    Step 2 — Wire ResetPasswordDialog into UsersPageClient.tsx:

    In UsersPageClient.tsx:
    - Import ResetPasswordDialog from @/components/settings/ResetPasswordDialog
    - Add state: resetTarget (type { id: string; name: string } | null), initialized to null
    - For each user row in the table that has a "Deactivate" or "Edit" action, add a "Reset Password" button or icon button that sets resetTarget to { id: user.id, name: user.name }
    - Render <ResetPasswordDialog open={resetTarget !== null} onOpenChange={(o) => { if (!o) setResetTarget(null) }} userId={resetTarget?.id ?? ''} userName={resetTarget?.name ?? ''} onSuccess={() => { setResetTarget(null); queryClient.invalidateQueries(['users']) }} /> at the bottom of the component, outside the table (alongside existing UserDialog if present)

    Only add ResetPasswordDialog next to rows where canAdmin is true (the existing canAdmin guard
    from usePermission already controls this). Do not change any existing create/edit/deactivate logic.
  </action>
  <verify>
    <automated>
      node -e "const c=require('fs').readFileSync('components/settings/ResetPasswordDialog.tsx','utf8');console.log(c.includes('export interface ResetPasswordDialogProps')?'HAS_PROPS':'MISSING_PROPS');console.log(c.includes('reset-password')?'HAS_ENDPOINT':'MISSING_ENDPOINT');console.log(c.includes('Passwords do not match')?'HAS_VALIDATION':'MISSING_VALIDATION')"
    </automated>
    Expected output: HAS_PROPS, HAS_ENDPOINT, HAS_VALIDATION (one per line).
  </verify>
  <done>
    - components/settings/ResetPasswordDialog.tsx exists and exports ResetPasswordDialogProps and the default component
    - Component validates password length and confirmation match client-side before calling API
    - Component calls POST /api/v1/users/${userId}/reset-password
    - Error from API is shown in the dialog
    - UsersPageClient.tsx imports and renders ResetPasswordDialog with resetTarget state
    - "Reset Password" action is available in user table rows alongside existing actions
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /settings/users Server Component | Server-side session check; no client code runs for unauthorized users |
| browser → ResetPasswordDialog form | Client-side validation only; server enforces auth + permissions at the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Elevation of Privilege | /settings/users page | mitigate | Server Component guard: hasPermission(session.permissions, 'users', 'read') before rendering client component |
| T-20-08 | Information Disclosure | Access-denied page | accept | Shows only "Access Denied" — no user data, no role details leaked |
| T-20-09 | Elevation of Privilege | Sidebar Users nav item visibility | mitigate | isPermitted() filter using hasPermission(permissions, 'users', 'read') — Accountant role hides the item |
| T-20-10 | Spoofing | ResetPasswordDialog API call | accept | The server endpoint re-validates auth and permission independently; client-side guard is defense-in-depth only |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan |
</threat_model>

<verification>
After all three tasks:

1. grep -n "isPermitted" components/layout/AppSidebar.tsx → at least 2 lines (definition + filter usage)
2. grep -c "use client" "app/(app)/settings/users/page.tsx" → 0 (Server Component)
3. grep -c "hasPermission" "app/(app)/settings/users/page.tsx" → 1
4. grep -c "use client" "app/(app)/settings/users/UsersPageClient.tsx" → 1
5. File exists: components/settings/ResetPasswordDialog.tsx
6. grep -c "reset-password" components/settings/ResetPasswordDialog.tsx → at least 1
7. grep -c "ResetPasswordDialog" "app/(app)/settings/users/UsersPageClient.tsx" → at least 1 (import)
</verification>

<success_criteria>
- An Accountant user navigating to /settings/users sees "Access Denied" — not the user list and not the login page
- The Users nav item is absent from the sidebar for Accountant-role sessions (sidebar receives empty permissions or permissions lacking users.read)
- The Users nav item is visible in the sidebar for Owner-role sessions (after Phase 20 Plan 01 ensures permissions are in the JWT)
- Owner can click "Reset Password" on a user row, enter and confirm a new password in the dialog, and submit successfully
- Client-side validation in the dialog catches passwords shorter than 8 characters and mismatched confirmations before making any API call
</success_criteria>

<output>
Create .planning/phases/20-user-management/20-03-SUMMARY.md when done
</output>
