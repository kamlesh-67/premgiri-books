---
phase: 20-user-management
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - app/api/v1/users/[id]/route.ts
  - app/api/v1/users/[id]/reset-password/route.ts
autonomous: true
requirements:
  - USER-03
  - USER-04

must_haves:
  truths:
    - "PATCH /api/v1/users/:id with { isActive: false } returns 400 when the caller is deactivating their own account"
    - "POST /api/v1/users/:id/reset-password hashes the new password and writes it atomically with an audit log entry"
    - "POST /api/v1/users/:id/reset-password returns 404 when the target user belongs to a different company"
    - "POST /api/v1/users/:id/reset-password returns 400 when the caller is resetting their own password"
    - "POST /api/v1/users/:id/reset-password returns 401 without a valid session"
    - "POST /api/v1/users/:id/reset-password returns 403 when caller lacks users.admin permission"
  artifacts:
    - path: "app/api/v1/users/[id]/route.ts"
      provides: "Self-deactivation guard in PATCH handler"
      contains: "You cannot deactivate your own account"
    - path: "app/api/v1/users/[id]/reset-password/route.ts"
      provides: "Password reset POST handler with bcrypt + transaction + audit log"
      exports: ["POST"]
  key_links:
    - from: "app/api/v1/users/[id]/reset-password/route.ts"
      to: "prisma.$transaction"
      via: "tx.user.update + tx.auditLog.create"
      pattern: "prisma\\.\\$transaction"
    - from: "app/api/v1/users/[id]/reset-password/route.ts"
      to: "requirePermission"
      via: "lib/utils/requirePermission"
      pattern: "requirePermission\\(session"
---

<objective>
Deliver the two backend changes that complete the server-side user management capability:

1. Add a self-deactivation guard to the existing PATCH handler in users/[id]/route.ts so an
   Owner cannot accidentally lock themselves out by setting their own isActive to false.

2. Create the new POST /api/v1/users/[id]/reset-password endpoint so an Owner can reset
   another user's password directly from the Users page without any email flow.

Purpose: These are the missing API capabilities for USER-03 and USER-04. Everything else
(create, list, deactivate the user record) already exists in the codebase.

Output: Modified users/[id]/route.ts with self-deactivation guard; new reset-password/route.ts.
</objective>

<execution_context>
@D:\My\BPG\design-inspirations-main\.claude\get-shit-done\workflows\execute-plan.md
@D:\My\BPG\design-inspirations-main\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\My\BPG\design-inspirations-main\.planning\PROJECT.md
@D:\My\BPG\design-inspirations-main\.planning\ROADMAP.md
@D:\My\BPG\design-inspirations-main\.planning\STATE.md
@D:\My\BPG\design-inspirations-main\.planning\phases\20-user-management\20-01-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns extracted from app/api/v1/users/[id]/route.ts -->

Auth + permission pattern (used in this file, copy for reset-password route):
  const session = await getSessionFromRequest(request)
  if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  const forbidden = requirePermission(session, 'users', 'admin')
  if (forbidden) return forbidden

IDOR guard pattern (line 58-62):
  const existing = await prisma.user.findUnique({ where: { id: userId, companyId } })
  if (!existing) return NextResponse.json({ error: 'Not found' }, { status: 404 })

Self-guard pattern from DELETE handler (line 120-124):
  if (userId === session.userId) {
    return NextResponse.json({ error: 'You cannot delete your own account.' }, { status: 400 })
  }

$transaction + audit log pattern (lines 80-98):
  await prisma.$transaction(async (tx) => {
    await tx.user.update({ where: { id: userId }, data: { ... } })
    await tx.auditLog.create({
      data: {
        companyId,
        userId: session.userId,
        entity: 'User',
        entityId: userId,
        action: 'UPDATE',
        oldValue: {},
        newValue: {},
        ipAddress: request.headers.get('x-forwarded-for') ?? null,
      },
    })
  })

Params type pattern (all routes in this directory):
  type Params = { params: Promise<{ id: string }> }
  export async function POST(request: NextRequest, { params }: Params) {
    const { id: userId } = await params
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add self-deactivation guard to PATCH handler</name>
  <files>app/api/v1/users/[id]/route.ts</files>
  <read_first>
    - app/api/v1/users/[id]/route.ts (full file — identify: PATCH handler start, the Zod parse block, the IDOR guard location after parse, and the DELETE handler's self-guard at lines 120-124 which is the model to copy)
  </read_first>
  <action>
    In the PATCH handler, after the Zod safeParse success check and after the IDOR guard
    (where `existing` is confirmed to not be null), add the following guard before the
    $transaction call:

      // Prevent self-deactivation — mirrors the self-delete guard in DELETE handler
      if (parsed.data.isActive === false && userId === session.userId) {
        return NextResponse.json(
          { error: 'You cannot deactivate your own account.' },
          { status: 400 }
        )
      }

    The guard must:
    - Only trigger when parsed.data.isActive is explicitly false (not undefined, not true)
    - Use userId (extracted from await params) and session.userId for comparison
    - Return 400 with message "You cannot deactivate your own account."

    Do NOT touch any other part of the file. Do NOT change the DELETE handler, the GET handler,
    or the blockUser() call — Redis removal is Phase 21's scope.
  </action>
  <verify>
    <automated>
      grep -n "You cannot deactivate your own account" "app/api/v1/users/[id]/route.ts"
    </automated>
    Must return exactly 1 matching line.
  </verify>
  <done>
    - PATCH handler contains the self-deactivation guard
    - Guard checks parsed.data.isActive === false AND userId === session.userId
    - Returns 400 with "You cannot deactivate your own account."
    - No other changes to the file
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create password reset endpoint</name>
  <files>app/api/v1/users/[id]/reset-password/route.ts</files>
  <read_first>
    - app/api/v1/users/[id]/route.ts (full file — extract the exact import block, auth pattern, IDOR pattern, and $transaction pattern; this is the direct analog)
    - lib/utils/requirePermission.ts (confirm function signature: requirePermission(session, resource, action) → NextResponse | null)
    - lib/session.ts (confirm getSessionFromRequest signature and return type)
    - prisma/schema.prisma (confirm auditLog model field names: companyId, userId, entity, entityId, action, oldValue, newValue, ipAddress)
  </read_first>
  <action>
    Create a new file at app/api/v1/users/[id]/reset-password/route.ts. This file must:

    Imports (copy from analog):
    - Import getSessionFromRequest from @/lib/session
    - Import prisma from @/lib/prisma
    - Import requirePermission from @/lib/utils/requirePermission
    - Import NextResponse from next/server
    - Import NextRequest type from next/server
    - Import z from zod
    - Import bcrypt from bcryptjs

    Zod schema (new, specific to this endpoint):
      const schema = z.object({
        password: z.string().min(8),
      })

    Type for Next.js dynamic params:
      type Params = { params: Promise<{ id: string }> }

    Export a POST handler:
    1. Authenticate: getSessionFromRequest(request) → 401 if null
    2. Permission check: requirePermission(session, 'users', 'admin') → return if forbidden
    3. Extract userId from await params; extract companyId from session.companyId
    4. IDOR guard: prisma.user.findUnique({ where: { id: userId, companyId } }) → 404 if null
    5. Self-reset guard: if (userId === session.userId) → 400 with message
       "Use account settings to change your own password."
    6. Parse request body with schema.safeParse() → 422 with details if invalid
    7. Hash: const passwordHash = await bcrypt.hash(parsed.data.password, 10)
    8. Transaction:
       a. tx.user.update({ where: { id: userId }, data: { passwordHash } })
       b. tx.auditLog.create with entity: 'User', entityId: userId, action: 'UPDATE',
          oldValue: {}, newValue: { passwordReset: true } — NEVER log the password or old hash
    9. Return NextResponse.json({ ok: true })

    Critical rules:
    - NEVER include passwordHash in any response body
    - NEVER log the old or new password hash in the audit log — use newValue: { passwordReset: true }
    - NEVER call blockUser() in this endpoint — password reset does not deactivate the session
    - The companyId in the IDOR guard MUST come from session, never from request params
    - Use bcrypt.hash(password, 10) — 10 rounds (setup uses 12; the login route uses 10 for PATCH/POST; match the users route pattern)
  </action>
  <verify>
    <automated>
      node -e "const r=require('fs');const c=r.readFileSync('app/api/v1/users/[id]/reset-password/route.ts','utf8');console.log(c.includes('export async function POST')?'HAS_POST':'MISSING_POST');console.log(c.includes('passwordReset: true')?'HAS_AUDIT_MARKER':'MISSING_AUDIT_MARKER');console.log(!c.includes('passwordHash') || c.indexOf('passwordHash')===c.lastIndexOf('passwordHash')?'HASH_USED_ONCE_ONLY':'WARN_HASH_IN_RESPONSE')"
    </automated>
    Expected: HAS_POST on first line, HAS_AUDIT_MARKER on second line.
  </verify>
  <done>
    - File exists at app/api/v1/users/[id]/reset-password/route.ts
    - Exports POST function
    - Performs auth (401), permission check (403), IDOR guard (404), self-guard (400), Zod validation (422)
    - Uses bcrypt.hash inside prisma.$transaction alongside auditLog.create
    - audit log newValue is { passwordReset: true } — no hash
    - Response is { ok: true } — no user data returned
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /users/:id | Authenticated; permission check enforces users.admin |
| client → POST /users/:id/reset-password | Authenticated; permission check enforces users.admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Denial of Service | PATCH isActive=false | mitigate | Self-deactivation guard: userId === session.userId → 400 |
| T-20-04 | Tampering | reset-password IDOR | mitigate | IDOR guard: findUnique with companyId from session prevents cross-company reset |
| T-20-05 | Information Disclosure | audit log password hash | mitigate | newValue: { passwordReset: true } — hash never logged |
| T-20-06 | Elevation of Privilege | reset-password without permission | mitigate | requirePermission(session, 'users', 'admin') → 403 before any DB access |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan |
</threat_model>

<verification>
After both tasks:

1. grep -n "You cannot deactivate your own account" "app/api/v1/users/[id]/route.ts" → 1 match
2. File exists: app/api/v1/users/[id]/reset-password/route.ts
3. grep -c "export async function POST" "app/api/v1/users/[id]/reset-password/route.ts" → 1
4. grep -c "passwordReset: true" "app/api/v1/users/[id]/reset-password/route.ts" → 1
5. grep -c "blockUser" "app/api/v1/users/[id]/reset-password/route.ts" → 0
</verification>

<success_criteria>
- An Owner sending PATCH { isActive: false } with their own userId receives 400 "You cannot deactivate your own account."
- POST /api/v1/users/:id/reset-password with valid Owner session, valid target userId, and valid { password: "minimum8" } returns 200 { ok: true }
- POST /api/v1/users/:id/reset-password with no cookie returns 401
- POST /api/v1/users/:id/reset-password with Accountant session returns 403
- Password hash never appears in any API response or audit log entry from this endpoint
</success_criteria>

<output>
Create .planning/phases/20-user-management/20-02-SUMMARY.md when done
</output>
