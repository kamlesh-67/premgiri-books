---
phase: 18-local-authentication
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - app/(auth)/login/page.tsx
  - app/(auth)/company-select/page.tsx
  - app/(app)/layout.tsx
  - components/providers/Providers.tsx
  - app/(app)/purchase-order/[id]/page.tsx
  - lib/utils/requirePermission.ts
  - lib/auth.ts
  - lib/auth.config.ts
  - app/api/auth/[...nextauth]/route.ts
  - package.json
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-04]

must_haves:
  truths:
    - "Login page submits to POST /api/v1/auth/login with fetch — no signIn from next-auth/react"
    - "Providers.tsx wraps children in QueryProvider and Toaster only — no SessionProvider"
    - "app/(app)/layout.tsx uses readSession() from @/lib/session — not auth() from @/lib/auth"
    - "requirePermission accepts JWTPayload (from @/lib/jwt) not Session from next-auth"
    - "lib/auth.ts and lib/auth.config.ts are deleted from the codebase"
    - "app/api/auth/[...nextauth]/ directory is deleted"
    - "next-auth and @auth/prisma-adapter are removed from package.json dependencies"
    - "grep for next-auth across the entire codebase returns zero matches"
  artifacts:
    - path: "app/(auth)/login/page.tsx"
      provides: "Login form using fetch to /api/v1/auth/login"
    - path: "components/providers/Providers.tsx"
      provides: "Client provider tree without SessionProvider"
    - path: "lib/utils/requirePermission.ts"
      provides: "403 helper typed against JWTPayload, not next-auth Session"
  key_links:
    - from: "app/(auth)/login/page.tsx"
      to: "app/api/v1/auth/login/route.ts"
      via: "fetch POST /api/v1/auth/login"
      pattern: "fetch.*api/v1/auth/login"
    - from: "app/(app)/layout.tsx"
      to: "lib/session.ts"
      via: "readSession() call"
      pattern: "readSession"
---

<objective>
Complete the NextAuth removal by updating client-facing pages, removing the SessionProvider, fixing the requirePermission type, deleting dead files (lib/auth.ts, lib/auth.config.ts, [nextauth] route), and uninstalling next-auth from package.json.

Purpose: AUTH-03 requires that `grep for next-auth across the codebase returns zero matches`. This plan achieves that final state. It runs in parallel with Plan 02 since they touch completely different files.
Output: next-auth removed from package.json. Zero next-auth imports anywhere. Login page works via plain fetch.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-local-authentication/18-RESEARCH.md
@.planning/phases/18-local-authentication/18-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 used by this plan -->

From lib/session.ts (Plan 01):
```typescript
export async function readSession(): Promise<JWTPayload | null>
```

From lib/jwt.ts (Plan 01):
```typescript
export interface JWTPayload {
  userId: string
  companyId: string
  roleId: string | null
  uiMode: 'simple' | 'advanced'
  permissions: Record<string, string[]>
}
```

Login API (Plan 01):
```
POST /api/v1/auth/login
Body: { email: string, password: string }
Success: 200 { ok: true } + sets httpOnly auth-token cookie
Failure: 401 { error: 'Incorrect email or password' }
```

Logout API (Plan 01):
```
POST /api/v1/auth/logout
Success: 200 { ok: true } + clears auth-token cookie (maxAge: 0)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update login page, company-select page, layout, Providers, and purchase-order page</name>
  <files>app/(auth)/login/page.tsx, app/(auth)/company-select/page.tsx, app/(app)/layout.tsx, components/providers/Providers.tsx, app/(app)/purchase-order/[id]/page.tsx</files>
  <read_first>
    - app/(auth)/login/page.tsx (current — uses signIn from next-auth/react)
    - app/(auth)/company-select/page.tsx (current — uses useSession, signOut from next-auth/react)
    - app/(app)/layout.tsx (current — uses auth() from @/lib/auth)
    - components/providers/Providers.tsx (current — wraps in SessionProvider)
    - app/(app)/purchase-order/[id]/page.tsx (current — uses useSession from next-auth/react)
    - .planning/phases/18-local-authentication/18-RESEARCH.md (Pattern 5 — useSession replacement strategy)
  </read_first>
  <action>
    Update app/(auth)/login/page.tsx: Remove `import { signIn } from 'next-auth/react'`. Replace the onSubmit function body: instead of `signIn('credentials', ...)`, call `const res = await fetch('/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: values.email, password: values.password }) })`. If `!res.ok` show toast.error('Incorrect email or password. Please try again.', { duration: 4000 }) and return. On success call `router.push('/company-select')` and `router.refresh()`. Keep all other UI code (form structure, shadcn components, loading state) exactly as is — only replace the submission logic.

    Update app/(auth)/company-select/page.tsx: Remove `import { useSession, signOut } from 'next-auth/react'`. Remove the `const { update } = useSession()` line. In handleSelectCompany: the function already calls POST /api/v1/auth/select-company — after Plan 01/02 that route now re-issues the JWT cookie, so remove the `await update({ companyId })` call entirely. After `verifyRes.ok` just call `router.push('/dashboard')` and `router.refresh()`. Find any `signOut()` call and replace with `await fetch('/api/v1/auth/logout', { method: 'POST' })` followed by `router.push('/login')`. Keep all other UI code intact.

    Update app/(app)/layout.tsx: Replace `import { auth } from '@/lib/auth'` with `import { readSession } from '@/lib/session'`. Replace `const session = await auth()` with `const session = await readSession()`. Replace `session.user.name` with `session ? (... name derivation using session.userId) : 'User'` — read the current layout to see exactly what fields it uses from session (companyId, name, initials). Since JWTPayload has userId/companyId but not name directly, derive initials from userId or add a DB lookup if name is needed. Per the current code, `name = session.user.name ?? session.user.email ?? 'User'` — since JWTPayload does not have name/email, replace with a simpler fallback: fetch the user name from DB using `prisma.user.findUnique({ where: { id: session.userId }, select: { name: true } })` and use that, OR simply use session.userId substring for the initials. Read the layout file carefully and make a pragmatic decision — the key requirement is no null reference errors and no next-auth import.

    Update app/(app)/purchase-order/[id]/page.tsx: Remove `import { useSession } from 'next-auth/react'`. Replace the `useSession()` call with a TanStack Query call to GET /api/v1/auth/me to get the current session user. If the component only uses `session.user.companyId`, replace the hook with `getSessionFromRequest` via a server action or just call the readSession() SSR pattern — read the current file first to determine how deeply it uses the session and choose the minimal replacement (direct API fetch via useQuery or convert to a server component if it only needs companyId for an API call).

    Update components/providers/Providers.tsx: Remove `import { SessionProvider } from 'next-auth/react'`. Remove the `<SessionProvider>` wrapper. Keep QueryProvider and Toaster exactly as they are. The result should be: `<QueryProvider>{children}</QueryProvider><Toaster position="bottom-right" richColors closeButton />`.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit --pretty false 2>&1 | grep -E "login|company-select|layout|Providers" | head -20 || echo "No errors in updated client files"</automated>
  </verify>
  <done>None of the four updated files import from 'next-auth' or 'next-auth/react'. Login page uses fetch to /api/v1/auth/login. SessionProvider is removed from Providers.tsx. layout.tsx uses readSession().</done>
  <acceptance_criteria>
    - grep "next-auth" app/(auth)/login/page.tsx app/(auth)/company-select/page.tsx app/(app)/layout.tsx components/providers/Providers.tsx returns empty output
    - app/(auth)/login/page.tsx contains the string "/api/v1/auth/login"
    - components/providers/Providers.tsx does NOT contain "SessionProvider"
    - app/(app)/layout.tsx contains "readSession" and does NOT contain "auth()"
    - pnpm tsc --noEmit exits 0 for these four files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix requirePermission type, delete dead files, remove next-auth from package.json</name>
  <files>lib/utils/requirePermission.ts, lib/auth.ts, lib/auth.config.ts, app/api/auth/[...nextauth]/route.ts, package.json</files>
  <read_first>
    - lib/utils/requirePermission.ts (current — types session as Session from next-auth)
    - lib/auth.ts (current — full file, needed to confirm no other consumers before deletion)
    - lib/auth.config.ts (current — needed to confirm no other consumers before deletion)
    - app/api/auth/[...nextauth]/route.ts (current — to confirm it's the old NextAuth handler)
  </read_first>
  <action>
    Update lib/utils/requirePermission.ts: Replace `import type { Session } from 'next-auth'` with `import type { JWTPayload } from '@/lib/jwt'`. Replace the function signature `session: Session` with `session: JWTPayload`. Replace `session.user.permissions` with `session.permissions`. The rest of the function body (calling hasPermission) stays the same. Verify the return type is still NextResponse | null.

    Delete lib/auth.ts: This file is being replaced by lib/jwt.ts + lib/session.ts. Confirm no other file imports from @/lib/auth (Plan 02 already handled api/v1/; this plan handles the remaining client files). Then delete the file.

    Delete lib/auth.config.ts: Used only by lib/auth.ts and the old middleware. Both are now gone. Delete the file.

    Delete app/api/auth/[...nextauth]/ directory: This directory contains the NextAuth catch-all route handler. With NextAuth removed, this route causes build errors. Delete the entire app/api/auth/ directory (containing only the [nextauth] subdirectory).

    Run package.json update: Execute `pnpm remove next-auth @auth/prisma-adapter` in the project root. Do NOT remove ioredis or @upstash/redis — those are deferred to Phase 21 per REQUIREMENTS.md CLOUD-02.

    After pnpm remove, verify next-auth is gone from package.json dependencies and devDependencies sections.

    Final check — run a codebase-wide grep: grep -r "next-auth" . --include="*.ts" --include="*.tsx" --exclude-dir=node_modules --exclude-dir=.next to confirm zero matches remain anywhere.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>pnpm build exits 0. grep -r "next-auth" . --include="*.ts" --include="*.tsx" --exclude-dir=node_modules --exclude-dir=.next returns empty output. lib/auth.ts, lib/auth.config.ts, and app/api/auth/[...nextauth]/ no longer exist.</done>
  <acceptance_criteria>
    - grep -r "next-auth" . --include="*.ts" --include="*.tsx" --exclude-dir=node_modules --exclude-dir=.next returns empty output (AUTH-03 requirement)
    - lib/auth.ts does not exist: test -f lib/auth.ts returns 1
    - lib/auth.config.ts does not exist: test -f lib/auth.config.ts returns 1
    - app/api/auth directory does not exist: test -d app/api/auth returns 1
    - cat package.json | grep next-auth returns empty (package removed)
    - lib/utils/requirePermission.ts imports JWTPayload from @/lib/jwt (not Session from next-auth)
    - pnpm build exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser fetch → /api/v1/auth/login | Login credentials submitted via client fetch; httpOnly cookie returned |
| Browser fetch → /api/v1/auth/logout | Clears cookie; no auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Spoofing | login page fetch | accept | fetch uses same-origin relative URL — no CORS issue in Electron; sameSite:lax cookie handles CSRF |
| T-18-10 | Elevation of Privilege | requirePermission type change | accept | Session → JWTPayload is a type-level change; hasPermission logic is unchanged; compiles cleanly |
| T-18-11 | Tampering | leftover next-auth route | mitigate | Delete app/api/auth/[...nextauth]/ immediately — if left in place it imports deleted next-auth and causes build failure; deletion is the mitigation |
| T-18-12 | Spoofing | logout (no auth required) | accept | Logout does not require auth — it just clears the cookie; no state changes occur; acceptable for desktop app threat model |
</threat_model>

<verification>
After all tasks across Plans 01, 02, and 03 are complete:

1. AUTH-01: curl -X POST http://localhost:3000/api/v1/auth/login -H 'Content-Type: application/json' -d '{"email":"test@test.com","password":"wrongpass"}' returns 401 {"error":"Incorrect email or password"}

2. AUTH-02: curl http://localhost:3000/api/v1/vouchers returns 401 {"error":"Unauthorized"} (no cookie)

3. AUTH-03 (critical): grep -r "next-auth" . --include="*.ts" --include="*.tsx" --exclude-dir=node_modules --exclude-dir=.next → empty output

4. AUTH-04: JWT roundtrip — sign payload with userId="u1", companyId="c1", roleId="r1" → verify → assert all three fields present

5. pnpm build → exits 0
</verification>

<success_criteria>
- Login page submits to /api/v1/auth/login via fetch, handles 401 error with toast
- Providers.tsx has no SessionProvider
- lib/auth.ts and lib/auth.config.ts are deleted
- app/api/auth/[...nextauth]/ directory is deleted
- next-auth removed from package.json
- grep for next-auth across the codebase returns zero matches (AUTH-03)
- requirePermission.ts accepts JWTPayload type from @/lib/jwt
- pnpm build exits 0
</success_criteria>

<output>
Create .planning/phases/18-local-authentication/18-03-SUMMARY.md when done
</output>
