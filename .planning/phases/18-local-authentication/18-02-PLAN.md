---
phase: 18-local-authentication
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - app/api/v1/vouchers/route.ts
  - app/api/v1/masters/ledgers/route.ts
  - app/api/v1/auth/me/route.ts
  - app/api/v1/auth/select-company/route.ts
  - app/api/v1/auth/user-companies/route.ts
autonomous: true
requirements: [AUTH-02, AUTH-04]

must_haves:
  truths:
    - "Every API route under app/api/v1/ uses getSessionFromRequest(request) instead of auth()"
    - "Every route returns 401 JSON when called with no auth-token cookie"
    - "The select-company route re-issues a full JWT cookie (not just 200 for client update)"
    - "No file under app/api/v1/ contains an import from @/lib/auth or next-auth"
    - "The me route returns roleName using getSessionFromRequest, not auth()"
  artifacts:
    - path: "app/api/v1/auth/me/route.ts"
      provides: "GET /api/v1/auth/me — returns roleName via new JWT session"
    - path: "app/api/v1/auth/select-company/route.ts"
      provides: "POST — verifies access, re-issues JWT with new companyId"
    - path: "app/api/v1/auth/user-companies/route.ts"
      provides: "GET — lists companies for authenticated user"
  key_links:
    - from: "app/api/v1/**/*.ts (85 files)"
      to: "lib/session.ts"
      via: "getSessionFromRequest import"
      pattern: "getSessionFromRequest"
    - from: "app/api/v1/auth/select-company/route.ts"
      to: "lib/jwt.ts"
      via: "signJWT re-issue"
      pattern: "signJWT"
---

<objective>
Replace all 85 `auth()` call sites in app/api/v1/ with `getSessionFromRequest(request)` from lib/session.ts. Update the three special auth routes (me, select-company, user-companies) individually since they have more complex logic than a simple search-replace.

Purpose: AUTH-02 requires every protected API route to validate JWT via the verifyJWT helper. This plan implements that for all routes, completing the server-side migration.
Output: Zero occurrences of `from '@/lib/auth'` or `from 'next-auth'` in any app/api/v1/ route file.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-local-authentication/18-RESEARCH.md
@.planning/phases/18-local-authentication/18-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From lib/session.ts (created in Plan 01):
```typescript
import type { NextRequest } from 'next/server'
export async function getSessionFromRequest(request: NextRequest): Promise<JWTPayload | null>
```

From lib/jwt.ts (created in Plan 01):
```typescript
export interface JWTPayload {
  userId: string        // was session.user.id in NextAuth
  companyId: string     // was session.user.companyId
  roleId: string | null // was session.user.roleId
  uiMode: 'simple' | 'advanced'
  permissions: Record<string, string[]>
}
export async function signJWT(payload: JWTPayload): Promise<string>
```

Pattern BEFORE in every route handler:
- Import: `import { auth } from '@/lib/auth'`
- Session: `const session = await auth()`
- Guard: `if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })`
- Fields: `session.user.companyId`, `session.user.id`, `session.user.roleId`, `session.user.permissions`

Pattern AFTER in every route handler:
- Import: `import { getSessionFromRequest } from '@/lib/session'`
- Session: `const session = await getSessionFromRequest(request)`
- Guard: unchanged
- Fields: `session.companyId`, `session.userId`, `session.roleId`, `session.permissions`

CRITICAL field rename: the new JWTPayload is FLAT (no .user nesting). session.user.X → session.X.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Mass search-replace of auth() in 82 non-auth API routes</name>
  <files>app/api/v1/**/*.ts (all routes except auth/me, auth/select-company, auth/user-companies)</files>
  <read_first>
    - app/api/v1/vouchers/route.ts (representative example — confirm exact pattern before mass replace)
    - app/api/v1/masters/ledgers/route.ts (second example to confirm session.user.companyId field names)
    - .planning/phases/18-local-authentication/18-RESEARCH.md (Migration Checklist section)
  </read_first>
  <action>
    First read app/api/v1/vouchers/route.ts and app/api/v1/masters/ledgers/route.ts to confirm the exact import line and field name patterns used. Then apply the following replacements to every .ts file under app/api/v1/ EXCEPT: app/api/v1/auth/me/route.ts, app/api/v1/auth/select-company/route.ts, and app/api/v1/auth/user-companies/route.ts.

    Replacement 1 — import line:
    Old: import { auth } from '@/lib/auth'
    New: import { getSessionFromRequest } from '@/lib/session'

    Replacement 2 — session call:
    Old: const session = await auth()
    New: const session = await getSessionFromRequest(request)

    Replacement 3 — field names (JWTPayload is flat, not nested under .user):
    Old: session.user.companyId
    New: session.companyId

    Old: session.user.id
    New: session.userId

    Old: session.user.roleId
    New: session.roleId

    Old: session.user.permissions
    New: session.permissions

    Old: session.user.uiMode
    New: session.uiMode

    After all replacements, scan for remaining @/lib/auth imports: grep -r "from.*@/lib/auth" app/api/v1/ --include="*.ts" -l

    Any file still importing @/lib/auth must be fixed manually. Also check for any session.user. references remaining (they indicate a missed field rename): grep -r "session\.user\." app/api/v1/ --include="*.ts"
  </action>
  <verify>
    <automated>pnpm tsc --noEmit --pretty false 2>&1 | grep -E "app/api/v1" | head -20 || echo "No TypeScript errors in app/api/v1"</automated>
  </verify>
  <done>grep -r "from.*@/lib/auth" app/api/v1/ --include="*.ts" returns no output. grep -r "session\.user\." app/api/v1/ --include="*.ts" returns no output. pnpm tsc --noEmit exits 0.</done>
  <acceptance_criteria>
    - grep -r "from.*@/lib/auth" app/api/v1/ --include="*.ts" -l produces empty output
    - grep -r "session\.user\." app/api/v1/ --include="*.ts" produces empty output (all fields renamed to flat)
    - grep -r "getSessionFromRequest" app/api/v1/ --include="*.ts" -l produces at least 80 file matches
    - pnpm tsc --noEmit exits 0 for files in app/api/v1/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update three special auth routes (me, select-company, user-companies)</name>
  <files>app/api/v1/auth/me/route.ts, app/api/v1/auth/select-company/route.ts, app/api/v1/auth/user-companies/route.ts</files>
  <read_first>
    - app/api/v1/auth/me/route.ts (current — uses auth(), must use getSessionFromRequest)
    - app/api/v1/auth/select-company/route.ts (current — calls auth() + returns 200 for client update(); must re-issue JWT)
    - app/api/v1/auth/user-companies/route.ts (current — read to see its pattern)
    - .planning/phases/18-local-authentication/18-RESEARCH.md (Pitfall 1 — select-company re-issue pattern; Pattern 5)
  </read_first>
  <action>
    Update app/api/v1/auth/me/route.ts: Replace `import { auth } from '@/lib/auth'` with `import { getSessionFromRequest } from '@/lib/session'`. Replace `const session = await auth()` with `const session = await getSessionFromRequest(request)` — add `request: Request` param to the GET handler signature (Next.js App Router allows optional request param on GET). Replace `session.user.roleId` with `session.roleId` and `session.user.companyId` with `session.companyId`.

    Update app/api/v1/auth/user-companies/route.ts: Apply the same import + call + field rename pattern.

    Update app/api/v1/auth/select-company/route.ts: This one requires more substantial changes. Read the current file to understand what it does. The new flow is: (1) call getSessionFromRequest(request) — return 401 if null; (2) parse body for companyId with Zod z.object({ companyId: z.string() }); (3) verify the user has access to that company: authDb.user.findFirst({ where: { id: session.userId, companyId: body.companyId, isActive: true } }) — return 403 if not found; (4) fetch the role permissions for this company context: authDb.role.findUnique if user.roleId exists; (5) call signJWT with the new companyId and all other fields from the current session, but companyId replaced; (6) build NextResponse.json({ ok: true }), set 'auth-token' cookie with same options as login route (httpOnly, sameSite:lax, path:/, maxAge: 7days), return response.

    Add imports for signJWT from @/lib/jwt and authDb from @/lib/authDb if not already present in select-company route.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit --pretty false 2>&1 | grep -E "app/api/v1/auth" | head -20 || echo "No TypeScript errors in auth routes"</automated>
  </verify>
  <done>All three routes compile without TypeScript errors. None import from @/lib/auth. select-company route calls signJWT and sets a new auth-token cookie.</done>
  <acceptance_criteria>
    - grep "from.*@/lib/auth" app/api/v1/auth/me/route.ts app/api/v1/auth/select-company/route.ts app/api/v1/auth/user-companies/route.ts returns empty output
    - app/api/v1/auth/select-company/route.ts contains signJWT and response.cookies.set('auth-token'
    - app/api/v1/auth/me/route.ts contains getSessionFromRequest and session.roleId (flat, not session.user.roleId)
    - pnpm tsc --noEmit exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cookie → getSessionFromRequest | auth-token JWT from client — verified by verifyJWT before any use |
| POST select-company body | companyId from client — must be verified against DB before re-issuing JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Elevation of Privilege | select-company route | mitigate | Verify user actually belongs to requested companyId via DB query before re-issuing JWT (T-04-03 pattern already present in original; preserve it) |
| T-18-07 | Spoofing | 85 API routes | mitigate | getSessionFromRequest returns null for missing/expired/tampered tokens; null check + 401 before any DB query in every route |
| T-18-08 | Information Disclosure | session.userId vs session.user.id | accept | Field rename is internal; no security impact — risk is compilation error not security breach; caught by tsc --noEmit |
</threat_model>

<verification>
After both tasks complete:
1. grep -r "from.*@/lib/auth" app/api/v1/ --include="*.ts" -l → empty
2. grep -r "from 'next-auth'" app/api/v1/ --include="*.ts" -l → empty
3. grep -r "session\.user\." app/api/v1/ --include="*.ts" → empty (all fields flat)
4. grep -r "getSessionFromRequest" app/api/v1/ --include="*.ts" -l | wc -l → at least 83
5. pnpm tsc --noEmit → exits 0
</verification>

<success_criteria>
- Zero @/lib/auth imports across all 85 app/api/v1/ route files
- Zero session.user.X field accesses in app/api/v1/ (all renamed to session.X flat fields)
- select-company re-issues a JWT cookie rather than returning 200 for client update()
- pnpm tsc --noEmit exits 0
</success_criteria>

<output>
Create .planning/phases/18-local-authentication/18-02-SUMMARY.md when done
</output>
