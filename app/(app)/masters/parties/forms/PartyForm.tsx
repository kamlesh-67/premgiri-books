// Stub — will be implemented in Phase 1 (Masters)
interface PartyFormProps {
  open?: boolean
  onOpenChange?: (open: boolean) => void
  initial?: Record<string, unknown> | null
}
export function PartyForm({ open: _open, onOpenChange: _onOpenChange, initial: _initial }: PartyFormProps) {
  return <div>PartyForm placeholder</div>
}
