// Stub — will be implemented in Phase 1 (Masters)
interface LedgerFormProps {
  open?: boolean
  onOpenChange?: (open: boolean) => void
  initial?: Record<string, unknown> | null
}
export function LedgerForm({ open: _open, onOpenChange: _onOpenChange, initial: _initial }: LedgerFormProps) {
  return <div>LedgerForm placeholder</div>
}
