// Stub — will be implemented in Phase 1 (Masters)
interface CategoryFormProps {
  open?: boolean
  onOpenChange?: (open: boolean) => void
  initial?: Record<string, unknown> | null
}
export function CategoryForm({ open: _open, onOpenChange: _onOpenChange, initial: _initial }: CategoryFormProps) {
  return <div>CategoryForm placeholder</div>
}
