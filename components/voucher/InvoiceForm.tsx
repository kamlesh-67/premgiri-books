// Stub — will be implemented in Phase 2 (Voucher Engine)
// The full InvoiceForm with double-entry accounting, GST calculation,
// and Prisma integration will replace this in Plan 02-xx.
export default function InvoiceForm({ mode }: { mode: 'sales' | 'purchase' }) {
  return <div className="p-6 text-sm text-muted-foreground">InvoiceForm ({mode}) — coming in Phase 2</div>
}
