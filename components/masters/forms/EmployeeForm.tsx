// Stub — will be implemented in Phase 6 (Payroll)
interface EmployeeFormProps {
  open?: boolean
  onOpenChange?: (open: boolean) => void
  initial?: Record<string, unknown> | null
}
export function EmployeeForm({ open: _open, onOpenChange: _onOpenChange, initial: _initial }: EmployeeFormProps) {
  return <div>EmployeeForm placeholder</div>
}
